Assignment 1 - chroot
Write a program to run  python or bash file in chroot jail securely.

    Create a new chroot jail.

    Check chroot is successes or not.

    Run a bash or Python script in the chroot jail. 

++++++++++++++++
#While being in the fake root mode, you can execute the inner_script manually 
#By running ( bash inner_script.sh ) or ( ./inner_script.sh )
